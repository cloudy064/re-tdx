# 股份回购纯 C++ 迁移与亮色主题

## 本次结果

- 从 `GFHG/GFNHGTJ/HGTJ/GGHG.sp` 和 `func_qxfa104`、`func_hgtj101`、
  `func_hgrztj101—104`、`func_gghg101/102` 恢复股份回购完整数据链。
- 新增纯 C++ `market repurchases` 和固定
  `/api/v1/market/repurchases`，运行时不依赖 Python。
- Svelte 新增 A 股方案、月度进度、年度统计、港股逐笔四视图，个股“更多”
  增加同票回购方案历史。
- 新增可持久化亮色主题；首屏预着色避免闪烁，K 线和估值图表会实时换色，
  用户重做后的布局保持不变。

## 数据与关联键

- A 股方案主表 `list/func_qxfa104_1.jsn` 当前 1,220 行、1,013 只证券；
  其中 177 只证券存在多期方案，所以单票历史从主表过滤，不伪造动态路径。
- 月度进度主表当前 13 个滚动统计点；年度全市场/A 股/港股各 5 年。
- 年度月度详情使用 `hgrztj21701/21702/21703/<年份>.jsn`。
- 港股最新主表当前 167 行；单票详情使用
  `gghg/<两位市场号><五位代码>.jsn`，市场号 `31` 与 `48` 均保留。
- `31:02378` 部署验收返回 496 条逐交易日回购历史。

## 实现边界

- 计划金额、实际金额、计划/实际股数统一规范为元和股；年度统计中的亿元、
  万股也在 C++ 层换算，网页不猜上游单位。
- 核心主表缓存 5 分钟，年度和港股动态详情缓存 15 分钟；零长度动态资源有
  负缓存，避免重复请求。
- A 股“是否完成”保留上游字段，金额完成率单独计算，不用完成率替代状态。

## 验证

- `npm run check`：0 错误、0 警告；生产构建成功。
- 原生测试拆成基础测试与股份回购测试两个目标，`ctest` 2/2 通过。
- `serve --self-test` 返回 `feature_count=31`。
- 真实命令验证了 A 股单票 5 期方案、13 个滚动月度点、2026 年 A 股 8 个
  月度统计点；最终运行服务中的港股详情已更新为 496 条历史。

## 后续

下一条公司行动主线优先选择股东增减持与股权质押：它们能继续与现有十大
流通股东、股东跨股票持仓和行业画像形成关联，而不是孤立增加一张列表。
