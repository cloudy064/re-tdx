# 公式全库审计与 IPO 上市辅导闭环

> 日期：2026-08-08。范围：非 L2 公开数据、纯 C++ 产品能力、固定 API 与 Svelte 数据中心。

## 解释器与资源基线

对 379 个客户端公式执行了带市场上下文和只读未来函数的全库审计：379/379 有
源码、语法受支持且可安全产生数值信号；231 个可直接执行，339 个在已有市场
上下文中可执行。354 个适用于平安银行日线样本的公式全部通过，另有 20 个只缺
券商/L2 等显式外部上下文、4 个不适用当前市场、1 个不适用当前周期；依赖缺失
和执行错误均为 0。显式上下文缺失继续保留为可解释边界，不以虚构数据补齐。

新增辅导资源前的变体审计为 617 个模板、486 个类型化模板、505 个已下载文件；
所有已下载文件均命中类型化入口，通用独占和解析错误均为 0。新增资源并补入
类型映射后，`recon jsn-discovery` 对当前 506 个文件得到 506/506 已识别且已
类型化、通用独占 0、解析错误 0。报告为
`output/tdx-jsn-discovery-ipo-guidance-closure.json`。

## 有界 709 探测

对 25 个 CFG 语义明确、尚未落盘的静态主表执行了只读元数据探测，15 个非空。
其中 `list/func_zdgz_qzkcbd101_1.jsn` 最大且语义最明确：1,134,752 字节，MD5
为 `95f73383e52f639395bf5349bb88fb0c`，客户端 CFG 定义为 IPO 上市辅导。
其余值得后续按独立业务继续固定的非空资源包括：债券申购 `kkzss101`、预测与
实际利润 `knzx101`、美股 IPO 日历/上市 `mgrl101/102` 与 `mgxg101/102`、知名
自然人持股 `nscg101`、配股 `qxfa107/108`、市场级转融资序列 `rzt101`、新股
申购详情 `sbxg101`、自由现金流 `xjl101` 和业绩反转/一致预期 `yjfz101`。

## 纯 C++ 上市辅导能力

下载并校验 `list/func_zdgz_qzkcbd101_1.jsn` 后，`market calendar` 新增
`--view ipo-guidance`，固定返回 1,259 家企业的公布日期、辅导进度、拟上市
板块、辅导机构、地区、公司简介、内部记录 ID 和完整原始行。当前板块分布为：
北交所 826、创业板 226、科创板 127、主板 80；主要进度包括辅导备案 689、
撤回辅导备案 268、辅导验收 225、辅导工作完成 33、总结报告 20、辅导中 14、
终止辅导 8。

辅导表、审核表和申购表是不同阶段的当前快照。按企业名精确相交时，辅导与审核、
辅导与申购均为 0；辅导表与证券目录的精确名称关联也为 0。因此接口不会用模糊
名称猜配证券，更不会把三个快照伪造成逐企业迁移历史。Svelte 财经日历新增
“上市辅导”页签，直接复用同一个类型化 API。

## 验证与正式服务

- 原生 CTest：96/96 通过；
- Svelte `check`：0 错误、0 警告，生产构建通过；
- 新增正式契约 `ipo-guidance-live`，单项 1/1 通过；
- 正式 API 全量契约：136/136 通过，报告为
  `output/api-contracts-ipo-guidance-full.json`；
- 正式服务：`127.0.0.1:8765`，PID `23392`；
- 安装版 SHA-256：
  `BD0336A95DFDAA92C563E63E474A25590A9D6E4887F8DC4AAE5670D600269271`。

全量契约首次执行时，平安银行 L1 快照连接被上游 7709 瞬时关闭，动态 PE/PB
缺价格而得到 135/136；该单项随即复核为 1/1，再次全量执行为 136/136。失败
报告被保留在执行记录中，最终交付报告为通过版本，不把上游瞬时错误误判成解析
缺陷。

## 下一批高收益方向

建议按“独立语义、非空规模、与现有能力互补”继续推进：先固定债券申购与配股，
再做预测/实际利润、自由现金流和业绩反转，最后处理美股 IPO 与知名自然人持股。
`rzt101` 当前只是市场级转融资序列，不能据此宣称已获得逐券转融券明细。
