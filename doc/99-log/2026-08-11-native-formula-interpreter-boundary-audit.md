# 公式解释器公开非 L2 边界复核

日期：2026-08-11

## 结论

最新完整证据 `output/native-formula-coverage-tcalc-boundary-v22.json` 显示：

- 379/379 个内置公式存在源码、语法可解析且数值信号安全；
- 数值降级为 0，90/90 个绘图公式已有 Render IR 和实体化语义；
- 解释器公开支持函数为 262 个；
- TCalc 静态注册表 390/390 已分类，其中 317 个为已识别实现，73 个为明确边界项；
- `remaining_public_non_l2_candidate_count` 为 0。

因此当前没有证据支持继续猜测新的普通公开函数语义。剩余注册项的边界为：

| 类型 | 数量 | 策略 |
|---|---:|---|
| L2 订单流 | 13 | 只接受已授权调用方提供的精确序列，不推测数据 |
| 实盘交易状态 | 38 | 只读工具不暴露交易执行状态 |
| 外部插件回调 | 15 | 不主动加载第三方 DLL |
| 券商私有信号 | 3 | 只接受调用方提供的精确序列 |
| 语法标记 | 4 | 作为 AND/OR 等语法项处理，不伪装成运行时函数 |

后续解释器工作应由新的 DLL 证据、合法 L2 数据输入或真实失败公式驱动，而不是继续按
名字补零值实现。该结论不表示整个通达信功能探索完成，只说明普通公开非 L2 公式注册
表已收口。
