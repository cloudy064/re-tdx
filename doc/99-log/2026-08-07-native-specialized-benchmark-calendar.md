# 2026-08-07 金融专项、相对基准与公司日历补齐

本轮继续处理非 L2 的高收益客户端资源，完成 `HYJYFX`、`JZFX` 和 `GSRL` 三族。

## 结果

- 新增纯 C++ `market specialized-metrics` 与
  `/api/v1/market/specialized-metrics`。银行 42 家、券商 49 家、保险 6 家；金额
  统一为元，小数比率同时给出百分比字段，券商同比从源字段复算。
- 新增纯 C++ `market benchmark-analysis` 与
  `/api/v1/market/benchmark-analysis`。28 张表覆盖十三段牛熊周期、行业/市场超额、
  停复牌及次新股表现；指定阶段只加载对应资源，网页列表默认省略 `raw`。
- 统一日历由 10 张扩展至 17 张资源，`GSRL` 公司事件达到 3,149 条。205 空壳行
  被过滤，207 配股保持上游缺失状态。
- 数据中心新增“金融专项指标”“相对基准分析”，个股工作台新增对应页签。
- JSN 覆盖为 420/616；439 个下载文件、384 个下载模板、0 解析错误、0 个已下载
  通用独占模板。
- 原生测试 77/77、Svelte 检查 0/0、生产构建通过，完整固定契约 112/112；
  强势股详情契约改为动态发现活跃区间，消除固定日期 ID 的自然过期问题。

## 语义边界

- `JZFX` 的 `$NOW`、当前涨幅和市值仍属于客户端宿主行情列，不冒充静态 JSN。
- 行业阶段的 881xxx 是客户端行业指数实体；名称无法从本地证券目录确认时保留代码。
- 日历 207 没有可下载元数据，不把“请求不到”解释成业务正常空表。
